spring:
  profiles:
    include: routers, jwt

  redis:
    host: 127.0.0.1
    port: 6379

logging:
  level:
    com.jackson0714.passjava: debug
    org:
      springframework:
        cloud:
          gateway: debug